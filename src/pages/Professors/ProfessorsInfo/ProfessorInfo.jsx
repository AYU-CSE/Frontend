import Header from "../../../components/Header/Header";

// 교수 소개 (main)
const ProfessorInfo = () => {
  return (
    <div>
      <Header />
      ProfessorInfo (main)
    </div>
  );
};

export default ProfessorInfo;
